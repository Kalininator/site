---
interface Props {
  date: Date
}

const { date } = Astro.props
---

<time class='my-4' datetime={date.toISOString()}>
  {
    date.toLocaleDateString('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric'
    })
  }
</time>
